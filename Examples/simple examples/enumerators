object Test extends Enumeration {
  protected case class testVal(x: Int, y: Int) extends super.Val{
    def testFunc: Double = Test.mult * x / (y * y)
  }
  val mult: Double = 0
  val SomeValue = testVal(12, 3)
}  

println(Test.values)
