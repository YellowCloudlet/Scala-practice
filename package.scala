package object home_app { //дополнительные фичи типа возможности вызова корня каким то другим символом

 /* implicit class IntAddinitional(s: Int) {
    def /^(Int): Int = scala.math.sqrt(s)
  }*/

  /*implicit class IntAddinitional(s: Int) {
    def \\(a: Int): Int = s + scala.math.sqrt(a)
  }*/

  /*case class A(i: Int)
  case class B(i: Int)

  implicit def aToB(a: A): B = B(a.i)

  val a = A(1)
  val b: B = a
  println(b)*/

}